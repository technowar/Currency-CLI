#!/usr/bin/env node

var program = require( 'commander' );

program
    .version( '0.0.0' )

program
    .command( 'hi' )
    .description( 'Initialize project configuration' )
    .action( function () {
        console.log( 'Wadap' );
    } )

program.parse( process.argv );
